# Environment variables for WebDashboardFrontend
# Update and copy to .env in the same folder before running locally.

# Base URL for the backend API (e.g., http://localhost:3002 or a deployed URL)
REACT_APP_BACKEND_BASE_URL=http://localhost:3002
